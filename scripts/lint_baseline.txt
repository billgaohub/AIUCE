# Lint 基线（I3 增量门禁）
#
# 捕获于 2026-07-17。该遗留代码库存在大量存量 lint 债务，
# 一次性清零既不可行也会引入高风险改动。因此采用「增量门禁」：
# CI 只在「新增 lint 错误数超过基线」时失败，不要求零错误。
#
# 若要主动偿还债务：修复若干错误后，把下方数字调低并重新提交基线即可。
#
# 统计范围：eleven_layer_ai/（见 scripts/lint_gate.sh）
FLAKE8_ERRORS=1579
MYPY_ERRORS=295
